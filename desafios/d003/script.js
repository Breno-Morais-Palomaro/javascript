function start() {
    var num = Number(prompt ('Digite um númeiro inteiro qualquer.'))
    var soma = num + 1
    var subt = num - 1
    Number(alert (`Antes do ${num}, temos o número ${subt} ____ Depois do ${num}, temos o número ${soma}.`))
}